# resolution/

Not implemented yet. Phase 3: entity resolution using the duplicate-pair classifier — embedding + cosine blocking to find candidate pairs, then real-time scoring against `models/duplicate_classifier`.
